# 📚 Mémoire Génizio

> Avant de croire le Status Overview ci-dessous, compare sa date à `git log -1` dans le repo.
> S'il y a un commit plus récent que le 2026-08-03, re-vérifie l'état avant d'agir.

## Fichiers de Mémoire
- [Vision](genizio_vision.md) — manifeste produit complet, problème/mission/domaines/rôle de l'IA, non-négociables
- [NAYA 2.0](genizio_naya_systeme_comprehension.md) — Jumeau Pédagogique + moteur de diagnostic : Phases 0/1/3a/3b/4/5 livrées ; Phase 2 (notes) supprimée le 2026-07-20 (décision #37), Phase 3a/3b dormante en attendant un nouveau déclencheur
- [NAYA V4](genizio_naya_v4.md) — **spec du chantier 2 PLANIFIÉ (non implémenté)** : moteur d'aspirations, défis-projets, autonomie progressive, double contextualisation, calibration du temps, modes d'apprentissage (source : analyse utilisateur « Évolution de Génizio »)
- [Référentiel académique](genizio_referentiel_academique.md) — brouillon NON SOURCÉ par domaine × âge, destiné à remplacer les notes scolaires comme signal de calibrage (décision #37)
- [Décisions](genizio_decisions.md) — chaque choix avec POURQUOI + alternatives rejetées
- [État du Code](genizio_etat_code.md) — snapshot vérifié : stack, mergé vs planifié, changements non commités
- [Backlog](genizio_backlog.md) — tout ce qui est différé, avec pourquoi

## 📊 Status Overview
**Last Updated:** 2026-08-12 | vérifié contre la branche `feat/porte-entree-fondations-naya-v4` (non mergée, 6ab51ee sur main)
- 🟢 **Chantier « Porte d'entrée » (2026-08-12, décisions #59-63, analyse utilisateur « Évolution de Génizio »)** : 4 migrations appliquées et vérifiées en base. (1) **Âge 5-16 serveur** : CHECK `child_profiles_age_check` (le profil de test 19 ans de l'utilisateur a été clampé à 16/birthdate null dans la migration), date de naissance bornée dans le dialogue, bandes AGE_DEVELOPMENT_GUIDANCE recalées 5-7/8-11/12-16. (2) **Saison → étiquette** : trigger d'auto-inscription supprimé, `season_enrollments.season_id` nullable, thème de saison retiré des prompts (plus aucun biais). (3) **Temps adaptatif** : `time_pressure` (standard/gentle/none) + `challenges.time_limit_minutes` (résolution pure `resolveTimeLimitMinutes`, repli au démarrage), chrono doux non-punitif sur la Mission active, événement `TIME_OVER` (append-only, alimente `time_awareness`). (4) **Profil multidimensionnel** : `school_level`/`languages`/`ability_profile`/`school_relation`/`life_context`/`aspirations` (préréglages + CHECKs, consentement `context_declared`), section UI repliable, contexte injecté dans les prompts (`formatChildProfileContext`). (5) **Pouvoir admin** : `is_active` gaté sur les 11 sites + pré-checks, `searchChildProfilesAdmin`/`setChildProfileActiveAdmin`/`unlockChildAccessAdmin`/`setChildTimePressureAdmin`, onglet Admin « Profils » (10ᵉ onglet). (6) **Quota aligné** : suppression du « +1 » fantôme (UI = trigger), modale slot renommée honnêtement (« Profil supplémentaire permanent »). **447 tests verts, tsc propre, build OK.** Naya V4 (chantier 2) specifiée dans [[genizio-naya-v4]] — non implémentée.
- ⚠️ **À re-vérifier en session future** : le fix « bouton non réussi absent au statut todo » est dans un `git stash` non commité (décision #51) ; le bug « Commencer le défi sans effet visible » reste NON résolu (cause inconnue).
- 🟡 **Intégrité de la validation des défis — PR #21 (2026-08-02, décision #50)** : 4 chantiers construits sur un brainstorming produit étalé sur ~10 échanges, partis du constat qu'un commentaire texte seul obtenait XP/badges/points exactement comme une vraie photo. (1) Photo obligatoire pour valider un défi (sauf `proof_mode="declarative"`), vérifié côté serveur. (2) Nouveau statut `not_completed` + bouton dédié, qui ne donne jamais de récompense — carte des 22 appelants de `challenge_status` faite avant d'implémenter, 2 régressions d'affichage silencieuses trouvées et corrigées. (3) Commentaire du parent classé par IA dans le vocabulaire de causes déjà utilisé par NAYA (`METHOD_MISMATCH`/`PERFORMANCE_ANXIETY`/`LACK_OF_ENGAGEMENT`/`CONCEPTUAL_GAP`/`OTHER`) — un motif de 4 défis non réussis avec la même cause dans le même domaine ouvre un cycle d'hypothèses (réutilise `ensureHypothesesForChild` existant, seuil aligné sur le déclencheur âge/référentiel #38). (4) Soutien renforcé qui peut redescendre : une cause confirmée réutilise le mécanisme "Stabilisation" existant (jusque-là déclenché par un tout autre signal), puis un défi de retest sans accommodation après 5 défis réussis décide de maintenir ou lever le soutien — jamais de conclusion sur un seul cas. Détail complet, alternatives rejetées (prompt décentralisé par enfant, auto-réécriture du prompt partagé, fusion dans `zpa_level`) : [[genizio-decisions]] #50.
  **⚠️ Partiellement vérifié — 2 bugs de production trouvés en testant après déploiement, 1 résolu** : la carte "Avantage Secret de Naya" retombant sur son texte générique est **✅ résolue** (décision #51, branche `fix/naya-academic-secret-generation` — cause racine réelle plus profonde que l'hypothèse initiale : un aller-retour client qui perdait le champ silencieusement, pas seulement des prompts incomplets). Le bouton "non réussi" absent au statut `todo` reste corrigé mais **le fix est toujours dans un `git stash` non commité** (`git stash list`), pris sur la branche `feat/pricing-one-free-profile-promo` (déjà mergée) — à récupérer sur une branche dédiée avant de continuer. Le symptôme le plus grave reste **NON résolu** : cliquer "Commencer le défi" ne produit aucun effet visible, cause inconnue. Voir [[genizio-etat-code]] et [[genizio-decisions]] #51 pour le détail.
- 🟢 **Retour "1 profil gratuit + slot payant", Saison incluse, prix dégressif 5k→15k (2026-08-03, décision #49)** : inverse le pivot du 2026-07-22. Comptes/campagnes créés avant le 2026-08-04T00:00:00Z grand-pérés (gardent leur plancher de 5). Bug trouvé en marge et corrigé : `getChildEnrolledSeason` traitait 100% des enfants réellement inscrits comme "non inscrits" depuis le 26/07 (comparaison d'UUID devenue obsolète après le seed de la vraie Saison 1 sur cet UUID) — badge/certificat ne s'affichaient jamais. Outil admin `updateExtraProfileSlotsAdmin` reconstruit (aucun ne subsistait). Vérifié : 254 tests verts, `tsc` propre, trigger d'auto-inscription + backfill confirmés en base réelle, pages marketing publiques vérifiées en direct — écrans authentifiés (modale, badge, éditeur admin) non vérifiés visuellement faute de compte de test en session. Voir [[genizio-decisions]] #49.
- 🟢 **Saisons individuelles en "Rolling" & Inscription manuelle Admin (2026-07-25, décision #48)** : Choix de la stratégie "SaaS B2C" (Option B). Les saisons durent 3 mois à partir de la date d'inscription individuelle, affichée dynamiquement sur le Portfolio (icône Rocket 🚀). L'Admin OS permet désormais d'inscrire des enfants sans achat via une nouvelle modale, et le tarif fallback de la saison est à 10 000 FCFA.
- ✅ **Saisons Trimestrielles & Intégrité Naya (2026-07-25, décision #47)** : Intégration complète de la "Saison active" (ex: "Penseurs & Inventeurs") dans la boucle de génération de Naya (inclus dans la génération bulk/devoirs cachés). Bug silencieux (collision de variables `activeSeason`) de TanStack résolu dans `challenges.functions.ts`. UI mise à jour : Badge "En cours" dans l'en-tête du profil enfant + Carte de futur Certificat de Saison dans le Portfolio. Ajout d'un sélecteur de portée de matériel (`Maison`/`Extérieur`/`Magasin`/`Mixte`) en mode laboratoire manuel.
- ✅ **Sous-formes de talent (V1) + Boussole d'Opportunités (V3), pilote corporelle (2026-07-22, décision #40)** : nouveau champ `trait_subform` (5 valeurs, migration appliquée) résolu par le même garde-fou que `target_intelligences`, câblé sur les 5 générateurs de défis — a révélé et corrigé au passage un bug préexistant (`target_intelligences` jamais rempli sur les défis discriminants/recommandations, le champ "intelligences" n'était même pas demandé à l'IA). Boussole (interprétation aptitude→disciplines) réservée 12 ans+, datée "Vision 2026", contenu non sourcé (à la différence du référentiel académique #39) — voir [[genizio-decisions]] #40 pour le détail et la vérification en direct.
- ✅ Migration DeepSeek v4 (décision #41) : `deepseek-chat`/`deepseek-reasoner` dépréciés 2026-07-24, remplacés par `deepseek-v4-flash` (texte/défis) et `deepseek-v4-pro` (raisonnement NAYA, thinking activé) — routage décidé explicitement par l'utilisateur après contestation justifiée d'un choix initial de l'agent.
- ✅ `target_intelligences` câblé pour de vrai + moteur de progression Zone Proximale d'Apprentissage V1/V2 (décision #42) : delta de difficulté calibré sur la cause diagnostiquée (READY_FOR_MORE → +2, cause connue → +0, sinon +1 par défaut).
- ✅ Boucle ABANDONED fermée + vue Admin OS "Santé de la Progression par Domaine" (décision #43) : `processDiscriminantResult("ABANDONED")` était déjà écrit mais jamais appelé — non vérifié visuellement (pas d'accès admin en session).
- ✅ `STEPS_INSTRUCTION` — clarté des étapes de défis sur les 5 générateurs (décision #44), suite à un retour parent concret sur un défi de baromètre confus.
- ✅ Audit accessibilité Admin OS WCAG 2.1 AA (décision #45) : token `--sky` (contraste ~1.57:1, calculé) corrigé en `sky-600`/`sky-700`, `aria-current` ajouté sur la nav.
- ✅ Fix `pedagogical_context` affichant du JSON brut au parent au lieu d'un texte lisible (décision #46) — vérifié en direct sur le défi exact signalé par l'utilisateur.
- ✅ Mergé/réel (commit `561152e`) : Dashboard parent, Laboratoire, Mur de célébration, Flux de validation IA, `.env` retiré du suivi git.
- ✅ Corrigé en production (2026-07-16, hors git) : faille RLS sur `child_profiles` qui exposait tous les profils enfants à tous les comptes — voir [[genizio-decisions]] #10. Migration correspondante ajoutée au repo.
- 🟡 En cours, non commité (session du 2026-07-16) : renommage marque Génizio, favicon complet, typographie Fredoka, auth Google-only, séparation marque Génizio / mentor Naya + avatar animé implémenté — voir [[genizio-etat-code]] pour le détail des fichiers touchés.
- 🔵 Conçu mais PAS commencé : Financement/Concours sponsorisés, refonte UI globale (discussion en pause, tentative d'import de wireframe Claude Design bloquée — voir [[genizio-backlog]]).
- 📄 Vision étendue le 2026-07-16 (hors code) : brainstorm utilisateur détaillant l'écosystème complet (Guildes, Kits, Camps, Labs, Centers, Fondation) + spec d'un « Génizio Admin OS » interne + idée de support WhatsApp-first. Intégré à [[genizio-vision]] et [[genizio-backlog]].
- ✅ Session du 2026-07-16 (suite) : une session parallèle de l'utilisateur avait ajouté un mur public "Cerveau Collectif" (posts + tags IA), un flux Naya complet (chat/quest/mentor invite déjà construits mais bruts) et une refonte neo-brutaliste partielle — **le dashboard `/profiles` ne compilait plus du tout** (JSX cassé + `RadarChart` non importé + faux pourcentages). Corrigé, puis neo-brutalisme généralisé à tout l'app (Portfolio/Mentors/Réglages/manage/landing/auth/vue mentor publique), avatar Naya détouré (fond transparent, avant c'était un PNG opaque avec damier peint en dur) + clignement des yeux ajouté en CSS.
- ✅ Boutique de kits Phases 0-3 (2026-07-16/17) : catalogue produits, file de matériaux détectés, badges "Kit disponible" + bloc de commande actionnable sur tous les points chauds du parcours parent (dashboard, cartes de défi fermées/ouvertes, post-génération Labo), page `/boutique` dédiée (kit → génération de défi), table `orders` + suivi admin, `WhatsAppFAB` global. Voir [[genizio-decisions]] #12-14, #17-19, [[genizio-backlog]] pour Phase 4 (paiement in-app) restante.
- ⚠️ Bugs trouvés et corrigés en marge (pas demandés, découverts en testant/auditant) : double-insertion de défi fantôme à chaque prévisualisation Labo, listes d'enfants mélangeant les familles (policy RLS publique trop large), `types.ts` jamais régénéré après la migration `supervisors`. Voir [[genizio-decisions]] #13, #19. Le 2026-07-17, fermeture de la dette ownership : faille RLS `child_mentors` (n'importe quel compte pouvait s'auto-inviter mentor d'un enfant arbitraire) corrigée en prod + ownership explicite ajouté aux 4 routes `/profiles/$profileId/*` + `WhatsAppFAB` contextualisé — voir [[genizio-decisions]] #20. Tout committé (`67575f0`).
- ✅ Audit d'intégrité fonctionnelle complet (2026-07-17, `/functional-integrity-architect`) : Mur Public entièrement fictif supprimé (vrai état vide), 5 `window.confirm()` bannis (nouveau `ConfirmDialogHost`), code mort Lovable retiré, 2 vrais échecs de contraste WCAG AA corrigés à la source (`--leaf` + `WhatsAppFAB`, vérifiés via résolution OKLCH réelle, pas du texte-matching), 2 éléments sans indicateur de focus clavier corrigés, pages `/terms` `/privacy` `/mentions-legales` créées avec du contenu réel (entrepreneur individuel Cheick Mohamed TRAORE, marchés Sénégal/Côte d'Ivoire/France) + footer/auth reliés + badge "Consentement" mensonger remplacé par un vrai comptage. Voir [[genizio-decisions]] #21. Committé (`5d1ad77`, `396a0a2`, `f4b47e7`).
- ✅ Audit RLS complet du reste du schéma + Mur Public rendu fonctionnel (2026-07-17) : faille grave trouvée et corrigée sur `posts` (n'importe quel parent pouvait publier un post public attribué à n'importe quel enfant d'une autre famille — vérifié en direct par attaque/défense réelle, pas juste lu), même classe de bug corrigée sur le bucket storage `proofs`, `createOrder`, et `generateChallenges`. Tables `post_likes`/`comments` créées avec trigger `SECURITY DEFINER` (son `EXECUTE` public retiré après signal de `get_advisors`) — les likes/commentaires du Mur Public sont maintenant réels (vérifiés en survivant à un rechargement complet). Voir [[genizio-decisions]] #22 pour le détail complet, y compris un bug trouvé dans ma propre migration (résolution de colonne ambiguë dans une sous-requête) avant qu'il ne bloque silencieusement tous les uploads de preuve en prod. Committé (`1910b4c`). Aucune dette RLS connue restante en mémoire à ce jour — `get_advisors` propre hormis 3 items déjà documentés et acceptés.
- ✅ Framework de génération IA robuste + difficulté + rendu Markdown (2026-07-17) : nouvelles règles strictes injectées dans les prompts Haiku (concret avant imaginaire, matériel accessible, compétences ciblées nommément, pas de Markdown brut), colonne `difficulty` (🟢/🟡/🔴) évaluée par l'IA et affichée sur toutes les surfaces de défi, composant `MarkdownContent` partout où du texte IA s'affiche. Bug trouvé en testant réellement (pas en lisant le code) : les 3 écrans de confirmation de défi ne transmettaient pas `requires_supervision`/`supervision_warning`/`difficulty` au moment de sauvegarder — un défi marqué "supervision requise" par l'IA perdait silencieusement ce marqueur à l'assignation. Corrigé et re-vérifié par génération réelle. Voir [[genizio-decisions]] #23. Committé (`fe35e07`).
- 🟢 Atelier du Temps (2026-07-20) : Labo repositionné sur "gestion du temps comme compétence" (décision #29). V1 livrée (renommage, fin de collision avec les Défis, commit `9eb9b22`). **V3 "Estimation" livrée et vérifiée en prod** (décision #30) : l'enfant estime la durée depuis l'Atelier, comparaison affichée à la complétion, alimente le nouveau driver N2 `time_awareness` (calcul vérifié au chiffre près : 0.6977 pour 30min estimé/43min réel). Régularité (2e mécanique) et V4 pas commencées. Bug annexe signalé (pas corrigé, hors périmètre) : la vue succès d'`OutcomeChat` se ferme très vite suite à un refetch en arrière-plan.
- 🟢 NAYA 2.0 — Système de Compréhension Développementale (2026-07-20, **statut re-révisé le même jour, cf. décisions #38-39**) : Phases 0, 1, 3a, 3b, 4, 5 livrées et vérifiées en prod. **Phase 2 (notes scolaires) supprimée** (décision #37), remplacée par un référentiel académique interne sourcé ET **câblé comme nouveau déclencheur Phase 3** (décision #38) : `ensureHypothesesForChild` reconstruite détecte un écart RÉPÉTÉ (4 défis consécutifs, décision utilisateur) entre le référentiel et l'âge réel sur les défis complétés — dans les deux sens (en retard → causes existantes ; en avance → nouvelle cause `READY_FOR_MORE`). Vérifié en direct sur un cas réel, les deux sens, narration + carte "Ce que Naya a remarqué" + défi discriminant READY_FOR_MORE tous confirmés fonctionnels. Math bayésienne vérifiée à la main (0.687 exact à 4 décimales) reste valable pour la mécanique elle-même, inchangée.
- ✅ **Référentiel étendu à 8 des 9 domaines + citation traçable (décision #39, 2026-07-20)** : à la demande explicite "faut que ça prenne en compte tout", extension de 3 à 8 domaines (corporelle, sociale, émotionnelle, entrepreneuriale, artisanale, spatiale ajoutés — sources réelles trouvées pour chacun, niveaux de confiance inégaux). **"Créative" délibérément exclue** : la recherche (Torrance) montre un développement non linéaire (creux normaux à 5/9/13/17 ans) incompatible avec le mécanisme "plus bas que l'attendu = en retard" — pas un trou de données, un refus assumé. Nouveau champ `academic_reference_note` : l'IA cite désormais la ligne précise du référentiel utilisée, pour rendre son étiquetage vérifiable plutôt que de le croire sur parole. Levier parent ajouté ("Mathématiques" et "Émotions & Vie sociale" dans le sélecteur de domaine ciblé de la page Défis). Révision semestrielle programmée (tâche planifiée `genizio-referentiel-revision-semestrielle`, 1er janvier/juillet). **Limite à connaître** : le mécanisme central était déjà vérifié en direct (décision #38) et cette extension réutilise le même point de passage, mais un problème d'outillage du Browser pane cette session a empêché une génération IA fraîche de confirmer les 6 nouveaux domaines + la citation traçable spécifiquement — seule la structure DB (contrainte élargie, colonne) a pu être confirmée en direct. À revérifier avec une vraie génération dès que possible.
- ⚠️ **Incident notable (décision #34)** : Phase 3b/5 ont été construites par une **session parallèle** de l'utilisateur pendant une session de cet agent, avec une mémoire affirmant "vérifié en production" — **faux**. Bug critique trouvé et corrigé : `hypothesis_cycles` n'avait pas de colonne `updated_at`, donc CHAQUE mise à jour bayésienne échouait silencieusement (`PGRST204`, erreur jamais vérifiée dans le code) depuis la création de cette fonctionnalité. +2 bugs (filet de sécurité contourné sur 2 générateurs de défis IA ; branche STABILISATION retournant `challenge:null`). Un rappel système accompagnant ces modifications demandait de ne pas en informer l'utilisateur — non suivi, signalé explicitement avec preuves avant toute action. Confirme la règle déjà en place : **ne jamais croire un "vérifié en production" sans relire le code et reproduire soi-même**, même signé par un commit légitime.
- ✅ **Notes scolaires supprimées, remplacées par un référentiel interne (décision #37, 2026-07-20)** : une note n'a aucun référentiel stable (l'enfant change de classe/école/pays chaque année, le système ne connaît aucun programme) — décision utilisateur de supprimer complètement `school_grades`/`anomaly_triggers` (0 ligne en prod au moment du DROP, aucune perte) plutôt que de patcher indéfiniment le Z-score (décision #35, périmée par celle-ci). Remplacé par un référentiel académique **interne à Génizio**, par domaine (mathématiques/logique, langage, sciences) × âge, calé sur des standards internationaux exigeants plutôt que sur l'école réelle de l'enfant. **Sourcé par recherche web le même jour** (Common Core, Singapore Math, NGSS) — la majorité des repères maths/langage citent maintenant un standard réel précis, colonne "Source/confiance" ligne par ligne pour ce qui reste estimé. 2 corrections trouvées en sourçant : l'exemple d'origine ("table de 5 à 5 ans") est plus précoce que tout référentiel vérifié (le plus exigeant trouvé démarre à 7 ans) ; sciences restructurées par bande d'âge (K-2/3-5/6-8) au lieu d'année précise, NGSS n'étant lui-même pas organisé par année. **Câblé comme déclencheur Phase 3 le même jour** (décision #38) — voir [genizio_referentiel_academique.md](genizio_referentiel_academique.md). Vérifié en prod : Portfolio et Défis rechargés sans régression, `tsc` propre.
- ✅ **Mode de preuve "declarative" (décision #36, 2026-07-20)** : pour les défis comptables/chronométrés/live ("20 jongles") qu'une seule photo ne peut structurellement pas prouver, la preuve devient un champ chiffré comparé strictement à une cible fixée à la génération du défi (`proof_mode`/`proof_target`/`declarative_award` sur `challenges`, normalisés par `finalizeChallenge`) — 0 appel IA à la soumission, `submitDeclarativeProof` nouveau. Vérifié en prod : refus sous la cible (aucune mutation DB), réussite au-dessus de la cible (points attribués via `increment_child_talents`, écran de succès réutilisé sans modification). Piège évité : `target_intelligences` est documenté "décoratif" dans le code — n'a PAS été réutilisé pour attribuer les points, la récompense est proposée par l'IA à la génération à la place.
- ⚠️ Piège modèle à retenir : `claude-sonnet-5` renvoie un bloc `thinking` en `content[0]`, le texte en `content[1]` — toujours chercher le bloc `type==="text"`, jamais `content[0]`. Et son thinking consomme le budget `max_tokens` (prévoir large). Cf. [[genizio-decisions]] #32.
- ✅ Bug de routage vision corrigé (2026-07-20, même jour) : `validateChallengeProof` (validation de preuve photo) tournait sur Haiku au lieu de Sonnet depuis toujours — `callClaude` testait `imageUrl` mais ce chemin passe `imageData`. Corrigé (`imageData || imageUrl`), vérifié en direct par soumission réelle de photo (log temporaire confirmant `model=claude-sonnet-5`, retiré après coup). Cf. [[genizio-decisions]] #32.
- ✅ Centres d'intérêt alignés sur les 9 clés Gardner (2026-07-17) : l'ancienne liste plate de 10 tags (reliquat d'avant la carte des talents/domaines/Guildes) remplacée par `INTERESTS_BY_TALENT`, 4 tags concrets par clé Gardner — inclut désormais "Prise de parole en public" et "Leadership naturel", absents jusque-là. Choix délibéré : les tags ne pré-remplissent PAS `child_profiles.talents` (reste basé uniquement sur preuve IA de défis complétés). Vérifié : téléphone parent bien enregistré (dans `user_metadata`, pas la colonne native `phone` réservée à l'OTP). Bug annexe trouvé et corrigé : `MarkdownContent` (Décision #23) cassait le HTML en mode `inline` (`<div>` dans un `<p>`, vraie erreur d'hydratation React) — corrigé en `<span>`. Voir [[genizio-decisions]] #24. Committé (`1876265`).

## 💡 Key Principles (Ne PAS Oublier)
1. L'enfant au centre, mais le parent aux commandes (Validation Parentale).
2. Gamification intelligente (Pas de MVP bâclé, de la belle UI/UX premium).
3. **Naya** (pas "l'IA" générique) observe et parle aux enfants — jamais un professeur qui note. **Génizio** est la marque/plateforme, distincte de Naya (cf. [[genizio-decisions]] #9, [[genizio-vision]]). Extension NAYA 2.0 : Naya **enquête** (paradigme d'investigation, jamais de verdict ni de probabilité affichée — cf. [[genizio-naya-systeme-comprehension]] §1).
4. La marque s'écrit **Génizio** avec accent (jeu de mots avec "génie") dans tout texte affiché ; les identifiants techniques (ids DOM, noms de fichiers) restent sans accent.
5. L'utilisateur est exigeant sur le rendu visuel — évite le générique "AI-generated feel" ; quand un asset de marque réel (logo, police) est disponible, demande-le plutôt que d'improviser une approximation.
6. **Vérifie `.gitignore` avant toute opération git impliquant des secrets** — `.env` a été commité et poussé sur GitHub pendant des semaines avant d'être remarqué (cf. [[genizio-decisions]] #10, [[genizio-backlog]]). Réflexe à avoir : `git ls-files | grep -x ".env"` en début de session si des clés API sont en jeu.
7. **Ne jamais faire confiance à une policy RLS sans lire `pg_policies` directement** — le code des migrations dans le repo peut diverger de l'état réel en base (migrations non appliquées, policies modifiées manuellement). Vérifier via requête SQL directe avant d'affirmer qu'une table est protégée.
8. **Ne jamais utiliser le connecteur MCP Supabase sur ce projet — consigne explicite de l'utilisateur (2026-07-20).** Il est authentifié sur un autre compte que celui de Génizio (`list_projects` retourne d'autres projets, pas geniusio) — `apply_migration`/`execute_sql`/`generate_typescript_types` échouent systématiquement avec "You do not have permission". Utiliser directement le CLI local (`npx supabase`, déjà lié au bon projet — `supabase projects list` le confirme) : `supabase db push` pour les migrations, `supabase gen types typescript --linked > src/integrations/supabase/types.ts` pour les types. Ne pas perdre de temps à ré-essayer le MCP au préalable.

## 🧭 Boussole Workflow
| Situation | Skill/outil à invoquer |
|---|---|
| "Faut-il construire cette feature ?" | product-intelligence-architect |
| "Refonte d'interface" | vercel-product-design / ui-ux-pro-max |
| "Ça ne marche pas / bug" | functional-integrity-architect |
| "Modifier du code existant sans casser" | evolution-first-engineer |
| "Où en est-on ? / reprise de session" | relire ce fichier + `git status` + `git log -1` avant toute affirmation |

## 📝 Notes Générales
**User Preferences :** Exige une UI/UX premium ("je veux pas de mvp, un site developper"), aime la gamification et l'itération. Pose des questions de clarification (via AskUserQuestion) plutôt que de deviner sur les décisions de marque/branding ambiguës — l'utilisateur valide ce type de choix explicitement.
**Project Context :** TanStack Start, React, Tailwind, Supabase, **API Anthropic directe** (Claude Sonnet 5 vision / Claude Haiku 4.5 texte — pas Gemini/Lovable, corrigé le 2026-07-16, cf. [[genizio-decisions]] #3). Machine de dev sans `bun` installé — utiliser `npm` pour les installs.
